slam_toolbox:
  ros__parameters:
    use_sim_time: false
    # Mode is overridden by slam_launch.py:
    #   - "localization" when loading a pre-existing map (default)
    #   - "mapping" when starting fresh
    mode: localization

    # Network delay handling
    transform_timeout: 0.2
    scan_buffer_size: 50
    minimum_time_interval: 0.5

    # Allow scans to be processed even when stationary
    # (critical for initial pose convergence and yaw drift correction).
    # Safe to keep at 0 because localization_slam_toolbox_node uses a
    # bounded LIFO buffer instead of an ever-growing pose graph.
    minimum_travel_distance: 0.0
    minimum_travel_heading: 0.0
    map_update_interval: 5.0

    # Rolling-buffer localization (localization_slam_toolbox_node only;
    # ignored by async_slam_toolbox_node in mapping mode).
    stack_size_to_use: 40
    localization_pose_search_distance: 2.0

    # Scan matching
    use_scan_matching: true
    use_scan_barycenter: true

    # Ceres solver configuration
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Correlation search (coarse scan alignment)
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Fine search (loop closure / refinement)
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Resolution matching the map
    resolution: 0.05
