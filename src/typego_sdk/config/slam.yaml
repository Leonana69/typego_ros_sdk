slam_toolbox:
  ros__parameters:
    use_sim_time: false
    mode: mapping  # Try mapping first
    
    # Network delay handling
    transform_timeout: 0.2
    scan_buffer_size: 25
    minimum_time_interval: 0.1

    minimum_travel_distance: 0.5
    map_update_interval: 1.0