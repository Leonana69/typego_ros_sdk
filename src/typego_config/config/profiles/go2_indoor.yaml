# go2_indoor — SLAM Toolbox + Nav2, tuned for enclosed spaces.
robot:
  type: go2
  name: go2-indoor
autonomy:
  type: base
profiles:
  nav2_params_file: nav2_params.yaml
  slam_params_file: slam.yaml
  local_planner_profile: dog
